-- haakjes gaan voor

-- true
-- SELECT * FROM Pres WHERE false AND true AND false OR true

-- false
-- NOT (FALSE OR TRUE AND FALSE)

-- true
-- TRUE AND FALSE OR NOT TRUE

-- insert into om data in db te zetten.
-- DELETE FROM om data uit db te zetten.
-- update set om data te wijzigen in db.
-- ALTER TABLE om tabel te wijzigen in db. (veld wijzigen, veld toevoegen, veld verwijderen)
-- CREATE INDEX * ON om een index te maken in db.

-- weg is weg geen undo in mysql db wordt door meerdere mensen gebruikt.
-- je kant niet data wijzigen die gebruikt wordt door andere, dus weg is weg.

-- index om performance te verbereren, kleine db maakt minder uit.